version: "3.9"
services:
  app:
    build: .
    image: cwa-app:latest
    ports:
      - "3000:3000"
    environment:
      PRISMA_PROVIDER: "sqlite"
      DATABASE_URL: "file:./dev.db"
    command: >
      sh -c "npx prisma generate || true &&
             npm start"
